import type { Metadata } from "next"; import { ComingSoon } from "@/components/ComingSoon"; import { futureMetadata } from "@/content/seo"; export const metadata: Metadata = futureMetadata("Workshop"); export default function Workshop(){return <ComingSoon name="Workshop" description="Learn by building. Practical environments for applying the SignalSmith method are taking shape." />}
