import type { Metadata } from "next"; import { ComingSoon } from "@/components/ComingSoon"; import { futureMetadata } from "@/content/seo"; export const metadata: Metadata = futureMetadata("Research"); export default function Research(){return <ComingSoon name="Research" description="Deep, evidence-driven investigations into important business, data, analytical, and technical questions." />}
