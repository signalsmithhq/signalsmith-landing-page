import type { Metadata } from "next"; import { ComingSoon } from "@/components/ComingSoon"; import { futureMetadata } from "@/content/seo"; export const metadata: Metadata = futureMetadata("Foundry"); export default function Foundry(){return <ComingSoon name="Foundry" description="SignalSmith's crowdsourced laboratory for real-world business problems. Bring us the problem. We'll find the signal." />}
