import type { Metadata } from "next"; import { ComingSoon } from "@/components/ComingSoon"; import { futureMetadata } from "@/content/seo"; export const metadata: Metadata = futureMetadata("Craft"); export default function Craft(){return <ComingSoon name="Craft" description="Reusable methods for solving problems well: frameworks, templates, checklists, playbooks, and techniques." />}
