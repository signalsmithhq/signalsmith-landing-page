import type { Metadata } from "next"; import { ComingSoon } from "@/components/ComingSoon"; import { futureMetadata } from "@/content/seo"; export const metadata: Metadata = futureMetadata("Blueprints"); export default function Blueprints(){return <ComingSoon name="Blueprints" description="Concrete designs for systems, data models, analytical solutions, and architectures. What should we build?" />}
