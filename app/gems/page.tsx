import type { Metadata } from "next"; import { ComingSoon } from "@/components/ComingSoon"; import { futureMetadata } from "@/content/seo"; export const metadata: Metadata = futureMetadata("Gems"); export default function Gems(){return <ComingSoon name="Gems" description="Small pieces of high-value reusable knowledge: metrics, concepts, distinctions, and decision principles extracted from the work." />}
